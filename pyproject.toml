[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "anonymous-long-video-qa"
version = "0.1.0"
description = "Anonymous reproducibility artifact for movie-level long-video QA"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]

