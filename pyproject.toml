[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "slomo-movie-level-qa"
version = "0.1.0"
description = "Movie-level joint inference for open-ended long-video QA"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
